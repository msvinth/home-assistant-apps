# Copilot Terminal tmux configuration

# Use the inherited terminal type (better compatibility)
set-option -g default-terminal $TERM

# Start window and pane numbering at 1
set-option -g base-index 1
setw -g pane-base-index 1

# Status bar styling - Copilot Terminal branded (GitHub palette)
set -g status-style "fg=colour252,bg=#0d1117"
set -g status-interval 15

# Left side: app identity label
set -g status-left "#[fg=#58a6ff,bold] Copilot Terminal #[fg=colour245]| "
set -g status-left-length 25

# Right side: auth status, HA status, date/time (via status script)
set -g status-right-length 60
set -g status-right "#(/opt/scripts/tmux-status.sh)"

# Window status styling
set -g window-status-current-style "fg=#58a6ff,bold"
set -g window-status-style "fg=colour245"

# Pane border styling
set -g pane-border-style "fg=colour236"
set -g pane-active-border-style "fg=#58a6ff"

# Message styling
set -g message-style "fg=#58a6ff,bg=#0d1117"

# Ensure environment variables propagate to status script
set -g update-environment "SUPERVISOR_TOKEN XDG_CONFIG_HOME HOME"

# Mouse support - only enable when NOT using ttyd
if-shell '[ -z "$TTYD" ]' {
    set -g mouse on
}

# OSC 52 clipboard support
set -g set-clipboard on
set -g allow-passthrough on

# Increase scroll-back buffer
set -g history-limit 50000

# Faster escape time for better vim/nano responsiveness
set -sg escape-time 0

# vi-style keys in copy mode
setw -g mode-keys vi

# Copy mode improvements
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind -n WheelDownPane select-pane -t= \; send-keys -M

bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-selection-and-cancel
bind -T copy-mode-vi DoubleClick1Pane select-pane \; send-keys -X select-word
bind -n DoubleClick1Pane select-pane \; copy-mode -M \; send-keys -X select-word
bind -T copy-mode-vi TripleClick1Pane select-pane \; send-keys -X select-line
bind -n TripleClick1Pane select-pane \; copy-mode -M \; send-keys -X select-line
